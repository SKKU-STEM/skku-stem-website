// Astro Content Collections 정의 — Publications / News / People 등은 다음 단계에서 추가
import { defineCollection } from 'astro:content';

export const collections: Record<string, ReturnType<typeof defineCollection>> = {};
